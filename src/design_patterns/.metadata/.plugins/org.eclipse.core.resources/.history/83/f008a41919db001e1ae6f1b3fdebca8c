package design_patterns.singleton_design_pattern;

import factory_design_pattern.Employee;

public class EmployeeDesignPattern {
    public Employee getEmployee(String type){
        if(type.trim().equalsIgnoreCase(""))
    }
}
